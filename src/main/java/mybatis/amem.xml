<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="amem">
	<insert id="memberInsert" parameterType="Amem">
		insert into amem values (#{id}, #{pass} , #{name},
		#{gender} , #{tel} , #{email} , #{picture}, #{numberid})
	</insert>
</mapper>